[metadata]
name = super-gcd 
version = 1.0.0
description = Super GCD application

[options]
zip_safe = False
packages = find:

[options.extras_require]
dev =
    pytest
    pylint
    black
    isort
    pyinstaller

[tool:pytest]
testpaths = tests
